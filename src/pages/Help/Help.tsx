import { Header } from '../../components';

const Help = ({ history }: any) => (
  <section>
    <Header history={history} />
    <main className="wrapper container_pt">
      <h1 className="title">Ayuda</h1>
    </main>
  </section>
);

export default Help;
